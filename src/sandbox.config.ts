// Read by the GitHub Pages workflow. An empty hash deploys the app to the repo root
// (https://<owner>.github.io/<repo>/), giving a clean shareable link.
export const sandboxOwner = 'dorailles'
export const sandboxHash = ''
