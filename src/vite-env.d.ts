/// <reference types="vite/client" />

// Provided by vite.config define(). Used by SpecSheet's source-handoff button to build a
// permalink to the repo; empty/false here falls back to a "no remote configured" note.
declare const __SANDBOX_SHA__: string
declare const __SANDBOX_REPO__: string
declare const __SANDBOX_DIRTY__: boolean
